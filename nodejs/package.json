{
  "name": "build-small-image",
  "description": "Build small image sample.",
  "version": "0.0.1",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/KennyChenFight/build-small-image"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "system-test": "mocha --exit test/*.test.js",
    "test": "npm run system-test"
  },
  "dependencies": {},
  "devDependencies": {
    "mocha": "^9.0.0",
    "supertest": "^6.0.0"
  }
}
